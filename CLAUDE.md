# CLAUDE.md — Project Conventions for pot-desktop

@AGENTS.md

## Claude Code

-   Follow the shared project instructions imported from `AGENTS.md`.
-   `AGENTS.md` is the single source of truth for this repository's conventions. Add new durable project rules there, not here, so every agent (Claude Code, Codex, Gemini CLI, Cursor) sees the same guidance.
-   Reminder — this repo is the fork `zeta987/pot-desktop`. Unless the user says otherwise in the current conversation, never open Pull Requests or Issues against the upstream `pot-app/pot-desktop` (or any other third-party fork). See the "Repository Policy — Fork-Only Contributions" section in `AGENTS.md`.
